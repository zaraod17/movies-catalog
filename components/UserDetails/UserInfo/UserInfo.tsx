import { StyledUserInfo } from "./UserInfo.styled";

const UserInfo: React.FC = () => {
  return (
    <StyledUserInfo>
      Info about user - avatar, email, username and movies preferences
    </StyledUserInfo>
  );
};

export default UserInfo;
